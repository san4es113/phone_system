<%= form_with(model: passlost, local: true) do |form| %>
  <% if passlost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(passlost.errors.count, "error") %> prohibited this passlost from being saved:</h2>

      <ul>
      <% passlost.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :trSeparation %>
    <%= form.text_field :trSeparation, id: :passlost_trSeparation %>
  </div>

  <div class="field">
    <%= form.label :carrierFrequency %>
    <%= form.number_field :carrierFrequency, id: :passlost_carrierFrequency %>
  </div>

  <div class="field">
    <%= form.label :txAntennaHeight %>
    <%= form.number_field :txAntennaHeight, id: :passlost_txAntennaHeight %>
  </div>

  <div class="field">
    <%= form.label :rxAntennaHeight %>
    <%= form.number_field :rxAntennaHeight, id: :passlost_rxAntennaHeight %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
